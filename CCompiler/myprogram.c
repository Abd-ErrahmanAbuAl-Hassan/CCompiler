double fun (int r){}
int main () {
    int x,y=1;
    // This is a single-line comment
    if (x == 42 || x ==2 && x< 3) {
        /* This is
           a block
           comment */
        x = x-3;
    } else { 
        y = 3.1; // Another comment
    }
  xnn(x+1);
 int a = 5  - * 10;
    return 0;
}
int xnn(int x , int d ){}

